{# templates/index.html.twig #}

<div class="flex-fill bg-secondary">

  {{ form_start(form) }}
  {{ form_row(form.file) }}
  {{ form_end(form) }}

  <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#resourceCreate">
    <span class="fa-solid fa-upload"></span>
    Upload
  </button>
</div>